import 'package:flutter/material.dart';

class EditDeviceViewBody extends StatelessWidget {
  const EditDeviceViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
